/** Package-manager install commands (shared across locales). */
export const brewCommands = ['brew tap devminson/emprint', 'brew install --cask emprint']

export const scoopCommands = [
  'scoop bucket add emprint https://github.com/devminson/scoop-emprint',
  'scoop install emprint'
]

/** Package-manager install commands (shared across locales). */
export const brewCommands = ['brew tap MinhyeongSon/emprint', 'brew install --cask emprint']

export const scoopCommands = [
  'scoop bucket add emprint https://github.com/MinhyeongSon/scoop-emprint',
  'scoop install emprint'
]
